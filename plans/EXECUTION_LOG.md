# Execution Log

<!-- ralph-loop appends one-line entries here after each phase action -->
2026-04-25 Phase 0 PASSED — engram_model.py unified, bench/run.py built with Modal L4 support, reproducibility gate passed (0.00e+00 loss diff), baseline locked: eval_cosine_top1=5.0%, grad_norm_p99=0.5610, bench/history/baseline.json.
2026-04-25 Phase 1 KILLED — LTI injection (LTIInjection, use_lti flag) added but killed by tests: 1-A PASS, 1-B PASS, 1-C FAIL (grad_norm_p99=0.5724 vs threshold 0.3927), 1-D FAIL (eval_cosine_top1=5.0% unchanged). LTI code retained in engram_model.py but disabled by default (use_lti=False). Proceeding to Phase 2 against baseline.
2026-04-25 Phase 2 KILLED — Loop-index embedding (sinusoidal, loop_dim=32) added but killed by tests: 2-A FAIL (ponder_steps_hist {"3": 1328}, halt gate insensitive to loop signal), 2-B FAIL (5.0% unchanged). Code retained with use_loop_idx=False default.
2026-04-25 Phase 5 PASSED — RoPE (precompute_rope_freqs + apply_rope, Q/K rotation, 4× buffer for extrapolation) replaces learned pos_embed. Tests: 5-A PASS (5.0% top1, within 0.5pp), 5-B PASS (5.0% at 2× ctx=64 and 3× ctx=96, zero cliff), 5-C FAIL (5.0% at ctx=64, metric plateau not RoPE failure). Bonus: grad_norm_p99 halved 0.561→0.280, zero extra params.
2026-04-25 Phase 6 PASSED — Locked use_rope=True as default in AttentionBrain. Updated README with measured deltas (RoPE: grad_norm_p99 −50%, extrap stable). LTI+loop-idx rows added showing null results. ENGRAM_OPENMYTHOS_TRANSFER_COMPLETE.
2026-04-26 v5 trained on Modal L4 (3 epochs, full 15 MB corpus, ~$3.60). Final loss 1.1448 (vs v4's 1.1347). Server live with v5_rope. Eval halted ENGRAM_COHERENT loop — diagnostic shows model produces ~constant output regardless of input (cos 1.000, ||Δ|| 1.6e-7 across distinct prompts). Top-5 nearest words to constant: the/i/bot/a/and. Structural bug, not undertraining. v4 & v5 share identical failure mode. Loop halted pending architecture investigation.
2026-04-26 ROOT CAUSE found via diag_block0.py: post-LN block 0 had attention softmax collapsed to near-uniform (max=0.033, all positions get equal weight) and FF output norm 360 vs residual 16 (residual signal drowned). Untrained model showed cos 0.62 healthy → trained collapsed to 1.000. Fix: switched to Pre-LN architecture (LN before sublayers) + AdamW(weight_decay=0.01) in ingest.py and bench/run.py.
2026-04-27 v6 trained on Modal L4 (3 epochs, same 15 MB corpus, Pre-LN + AdamW, ~$3.60). Final loss 1.0509 (vs v5 1.1448). Avg ponder 3.0 (vs v5 1.0). Diagnostic confirms fix: cos 1.000→0.960, ||Δ|| 1e-7→0.43. All 16 eval prompts produce distinct replies. Architecture bug FIXED. But output is still gibberish-quality with heavy Gutenberg novel vocab (ichthyosaurus/maachah/derbies/harringtons/_baron). Loop halted for user input on v7 (dailydialog-only ~$2) vs v8 (bigger model ~$13). Server now serves v6_preln.
2026-04-27 v7 trained on Modal L4 (5 epochs, dailydialog.txt only 6.1 MB, Pre-LN + AdamW, ~$3). Final loss 1.0101 — best yet. Vocab 38,062→18,187 (novels gone). Diagnostic: cos 0.960→0.912, ||Δ|| 0.43→4.51 (10× more differentiation than v6). Modal_train.py extended with BOOKS config field forwarded as ingest.py --books. Output still gibberish but novel-vocab replaced by dailydialog data artifacts (236/106/yw132/rikknen/bidpai/morrissette/pavarotti). Common English emerging but drowned by noise. Server now serves v7_dialog. Diagnosis: dailydialog has preprocessing residue requiring corpus cleanup OR bigger model OR different corpus.
